ignored:
  - DL3018  # Pin versions in apk add
  - DL3003  # Use WORKDIR to switch to a directory
  - DL3006  # Always tag the version of an image explicitly (ARG-based FROM)
  - DL3029  # Do not use --platform flag with FROM (required for buildx multi-arch)
